<?php
/**
 * Optional local database overrides (copy to config.local.php in this folder).
 *
 * Values you set here replace the defaults in config.php without editing the main file.
 * Useful for production passwords or when your MySQL user is not "root".
 *
 * Example:
 *   return [
 *     'user' => 'cirms_user',
 *     'pass' => 'your_actual_password',
 *     'users_password_column' => 'passwd', // if your `users` table does not use column name `password`
 *   ];
 */
return [
    // 'host' => '127.0.0.1',
    // 'name' => 'cirms',
    // 'user' => 'cirms_user',
    // 'pass' => 'your_secure_password_here',
    // 'users_password_column' => 'passwd',
];
